package com.nter.final_project.persistence.entity;

public enum StatusOrder {
    PENDING_PAYMENT,
    PROCESSING,
    AWAITING,
    SHIPPED,
    DELIVERED,
    COMPLETED,
    CANCELLED,
    RETURNED
}
